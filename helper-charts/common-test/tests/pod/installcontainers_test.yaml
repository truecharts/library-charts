suite: pod install containers
templates:
  - common.yaml
tests:
  - it: with explicit name should pass
    set:
      installContainers:
        install1:
          name: template-test
    asserts:
      - documentIndex: 0
        isKind:
          of: Deployment
      - documentIndex: 0
        equal:
          path: spec.template.spec.containers[1].name
          value: template-test

  - it: with implicit name should pass
    set:
      installContainers:
        install1:
          image: template-test
    asserts:
      - documentIndex: 0
        isKind:
          of: Deployment
      - documentIndex: 0
        equal:
          path: spec.template.spec.containers[1].name
          value: install1

  - it: with templated name should pass
    set:
      installContainers:
        install1:
          name: "{{ .Release.Name }}-container"
    asserts:
      - documentIndex: 0
        isKind:
          of: Deployment
      - documentIndex: 0
        equal:
          path: spec.template.spec.containers[1].name
          value: RELEASE-NAME-container
